const isEven = (n) => n % 2 === 0;// Using arrow function

let result = marks >= 35 ? "Pass" : "Fail";// Using ternary operator

const greet = (name) => {// Using default parameters
  let user = name ? name : "Guest";
  console.log(`Hello, ${user}`);
};



